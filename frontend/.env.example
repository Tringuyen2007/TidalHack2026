# Frontend environment variables
# Copy to .env.local and fill in values

# Backend API URL (the Next.js rewrite proxy destination)
BACKEND_URL=http://localhost:3001

# MongoDB URI (only needed if you keep the frontend seed script)
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/ili_alignment
# SESSION_SECRET=some-secret
